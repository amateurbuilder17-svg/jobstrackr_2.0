# Generated database types

`database.types.ts` is produced by the Supabase CLI. **Do not edit it by hand** —
the next regeneration discards your changes.

Regenerate after any migration:

```bash
pnpm db:types
```

That command builds the migrations on a throwaway Postgres container and reads
the types back out, so it works without touching the live project and without
needing credentials.
