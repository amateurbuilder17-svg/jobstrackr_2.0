/**
 * Shared facts for the legal pages.
 *
 * One place, because three pages carrying three separately-edited copies of a
 * contact address is how one of them ends up pointing at a mailbox nobody
 * reads. The date is written by hand rather than derived from the build: "last
 * updated" must mean the last time somebody changed the words, not the last
 * time the site was deployed.
 */
export const LEGAL_CONTACT = "contact@jobstrackr.in";

export const LEGAL_UPDATED = "4 September 2026";
